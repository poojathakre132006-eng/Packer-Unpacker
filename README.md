# Packer-Unpacker
The Packer Unpacker project is a Java-based application used to combine multiple files into a single packed file (Packer) and later extract the original files from that packed file (Unpacker)
